import type { FC } from 'react';

const PostCreate: FC = () => (
  <div role="region" aria-label="Create post screen" tabIndex={0} aria-live="polite">
    Hello from creation Post screen
  </div>
);

export default PostCreate;
